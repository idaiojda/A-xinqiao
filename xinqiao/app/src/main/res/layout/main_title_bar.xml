<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/title_bar"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:background="@android:color/transparent" >
    <TextView
        android:id="@+id/tv_back"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:background="@drawable/go_back_selector" />
    <!-- 居中标题（非课程页显示） -->
    <TextView
        android:id="@+id/tv_main_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        style="@style/TopBarTitleText"/>

    <!-- 课程页搜索框（默认隐藏，课程页显示） -->
    <EditText
        android:id="@+id/et_search_course"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:layout_toRightOf="@id/tv_back"
        android:layout_toLeftOf="@id/ll_play_history_top"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:hint="@string/search_course"
        android:singleLine="true"
        android:imeOptions="actionSearch"
        android:drawableLeft="@drawable/ic_search_blue"
        android:drawablePadding="8dp"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:background="@drawable/bg_searchbar_pill_selector"
        android:textColor="#4E5969"
        android:textColorHint="#99B8FF"
        android:textSize="14sp"
        android:visibility="gone"/>

    <!-- 删除外置搜索按钮，改为输入框右侧内置图标按钮 -->

    <!-- 课程页右侧播放历史（默认隐藏，课程页显示） -->
    <LinearLayout
        android:id="@+id/ll_play_history_top"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:paddingLeft="12dp"
        android:paddingRight="16dp"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:background="?attr/selectableItemBackground">
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_history"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="4dp"
            android:text="@string/play_history"
            android:textColor="#99B8FF"
            android:textSize="14sp"/>
    </LinearLayout>
    <TextView
        android:id="@+id/tv_save"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_alignParentRight="true"
        android:layout_marginTop="10dp"
        android:layout_marginRight="20dp"
        android:layout_centerVertical="true"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@android:color/white"
        android:text="@string/save"
        android:visibility="gone"
        />
</RelativeLayout>
