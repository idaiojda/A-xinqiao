<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 顶部栏统一标题样式：18sp、半粗/粗体、颜色跟随TopBarOnPrimary -->
    <style name="TopBarTitleText">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/topBarOnPrimary</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
        <item name="android:gravity">center</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- Tab 文本统一样式：13sp，取消全大写，正常字重 -->
    <style name="TabTextAppearance.Xinqiao" parent="@android:style/TextAppearance">
        <item name="android:textSize">13sp</item>
        <item name="android:textStyle">normal</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Modern Tab Text Appearance for Enhanced Title Bar -->
    <style name="TabTextAppearance.Modern" parent="@android:style/TextAppearance">
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">normal</item>
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:letterSpacing">0.02</item>
    </style>

    <!-- 弹窗动画样式：进入淡入+放大，退出淡出+缩小 -->
    <style name="MsgActionsPopupAnimation">
        <item name="android:windowEnterAnimation">@anim/popup_enter</item>
        <item name="android:windowExitAnimation">@anim/popup_exit</item>
    </style>
</resources>
